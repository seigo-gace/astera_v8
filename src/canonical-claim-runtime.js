'use strict';

const crypto = require('node:crypto');
const { normalizeEvidencePacket, tokenOverlap, unique } = require('./judgment-materials-analyzer');

const QUERY_ROLES = Object.freeze({
  OFFICIAL:'OFFICIAL', PRIMARY:'PRIMARY', INDEPENDENT:'INDEPENDENT', VERSION:'VERSION', COUNTER:'COUNTER',
  NUMERIC_LOWER:'NUMERIC_LOWER', NUMERIC_UPPER:'NUMERIC_UPPER', NUMERIC_ALTERNATE:'NUMERIC_ALTERNATE'
});
const CONFIRMED='CONFIRMED';
const UNDETERMINED='UNDETERMINED';

const normalizeText=(value)=>String(value||'').normalize('NFKC').replace(/\r\n?/g,'\n').replace(/[ \t]+/g,' ').trim();
function stable(value){if(Array.isArray(value))return`[${value.map(stable).join(',')}]`;if(value&&typeof value==='object')return`{${Object.keys(value).sort().map((key)=>`${JSON.stringify(key)}:${stable(value[key])}`).join(',')}}`;return JSON.stringify(value);}
function id(prefix,value){return`${prefix}_${crypto.createHash('sha256').update(stable(value)).digest('hex').slice(0,24)}`;}
function deepFreeze(value){if(!value||typeof value!=='object'||Object.isFrozen(value))return value;for(const child of Object.values(value))deepFreeze(child);return Object.freeze(value);}

function splitPlain(input,from,to,push){let start=from,quoteDepth=0;for(let i=from;i<to;i+=1){const ch=input[i];if(['「','『','“'].includes(ch))quoteDepth+=1;if(['」','』','”'].includes(ch))quoteDepth=Math.max(0,quoteDepth-1);if(quoteDepth===0&&/[。！？!?;；\n]/u.test(ch)){const part=input.slice(start,i+1);push(start,i+1,'PLAIN_CONTAINER','TEXT',/[「『“][\s\S]+[」』”]/u.test(part)?'QUOTED':'DIRECT');start=i+1;}}if(start<to){const part=input.slice(start,to);push(start,to,'PLAIN_CONTAINER','TEXT',/[「『“][\s\S]+[」』”]/u.test(part)?'QUOTED':'DIRECT');}}
function fragmentText(text,baseOffset=0){const input=String(text||''),fragments=[];let cursor=0,index=0;const push=(start,end,containerRole='PLAIN_CONTAINER',contentRole='TEXT',quotationRole='DIRECT')=>{const raw=input.slice(start,end),left=raw.length-raw.trimStart().length,right=raw.length-raw.trimEnd().length,from=start+left,to=end-right;if(to<=from)return;const normalized=normalizeText(input.slice(from,to));if(!normalized)return;index+=1;const span={start:baseOffset+from,end:baseOffset+to};fragments.push({fragment_id:id('frag',{span,normalized,containerRole,contentRole,quotationRole}),index,text:input.slice(from,to),normalized_fragment:normalized,span,source_axes:{container_role:[containerRole],content_role:[contentRole],quotation_role:[quotationRole]}});};for(const match of input.matchAll(/```[\s\S]*?```/gu)){if(match.index>cursor)splitPlain(input,cursor,match.index,push);push(match.index,match.index+match[0].length,'FENCED_CONTAINER','CODE_OR_QUOTED_BLOCK','DIRECT');cursor=match.index+match[0].length;}if(cursor<input.length)splitPlain(input,cursor,input.length,push);return deepFreeze(fragments);}

function assertionType(text){const value=normalizeText(text);if(!value)return'UNKNOWN';if(/[?？]$/u.test(value)||/^(?:何|なぜ|どう|いつ|どこ|誰|what|why|how|when|where|who)\b/i.test(value))return'QUESTION';if(/(?:しろ|せよ|してください|してくれ|実装しろ|確認しろ|変更しろ|do not|must not|please\s+(?:build|implement|verify|change)|\bimplement\b|\bverify\b)/i.test(value))return'COMMAND';if(/(?:と思う|かもしれない|たぶん|推測|maybe|probably|I think)/i.test(value))return'EVALUATION';return'ASSERTION';}
function modality(text){const value=normalizeText(text);if(/(?:場合|なら|ならば|とき|時は|if\b|when\b|provided that)/i.test(value))return'CONDITIONAL_RULE';if(/(?:可能|できる|can\b|may\b|possible)/i.test(value))return'POSSIBILITY';return'OBSERVED';}
function polarity(text){const value=normalizeText(text),neg=value.match(/(?:ない|ません|不可|禁止|ではない|じゃない|not\b|no\b|never\b|without\b)/gi)||[];if(neg.length>=2&&/(?:わけではない|とは限らない|not\s+impossible|not\s+un)/i.test(value))return'AFFIRMATIVE';return neg.length?'NEGATIVE':'AFFIRMATIVE';}
function timeScope(text){const value=normalizeText(text);if(/(?:現在|現行|最新|今日|今|current|latest|today|now)/i.test(value))return'CURRENT';if(/(?:昨日|過去|以前|当時|previous|past|yesterday)/i.test(value))return'PAST';if(/(?:今後|将来|予定|future|will\b)/i.test(value))return'FUTURE';return'UNKNOWN';}
function parseStructure(text,fallbackTarget=''){const value=normalizeText(text).replace(/[。！？!?]+$/u,'');let match=/^(.{1,120}?)(?:は|が)(.{1,180})$/u.exec(value);if(match)return{subject:normalizeText(match[1]),predicate:'IS',object_or_value:normalizeText(match[2]),resolved:true};match=/^(.{1,120}?)\s+(?:is|are|was|were|has|have|supports?|requires?)\s+(.{1,180})$/i.exec(value);if(match)return{subject:normalizeText(match[1]),predicate:'IS',object_or_value:normalizeText(match[2]),resolved:true};if(fallbackTarget)return{subject:normalizeText(fallbackTarget),predicate:'STATED_CONTENT',object_or_value:value,resolved:Boolean(value)};return{subject:'UNKNOWN',predicate:'UNKNOWN',object_or_value:value||'UNKNOWN',resolved:false};}

function claimFromFragment(fragment,task,{forced=false}={}){const type=assertionType(fragment.normalized_fragment);if(!forced&&type!=='ASSERTION')return null;const structure=parseStructure(fragment.normalized_fragment,task.target||'');const origin=fragment.source_axes.container_role.includes('FENCED_CONTAINER')?'CODE_STRUCTURE':fragment.source_axes.quotation_role.includes('QUOTED')?'ATTRIBUTED_ASSERTION':'DIRECT_ASSERTION';const payload={task_id:task.id,fragment_id:fragment.fragment_id,subject:structure.subject,predicate:structure.predicate,object_or_value:structure.object_or_value,polarity:polarity(fragment.normalized_fragment),modality:modality(fragment.normalized_fragment),time_scope:timeScope(fragment.normalized_fragment),jurisdiction:'UNKNOWN',version_scope:'UNKNOWN',claim_origin:origin};const passed=structure.resolved&&structure.subject!=='UNKNOWN'&&structure.predicate!=='UNKNOWN';return deepFreeze({...payload,claim_id:id('claim',payload),raw_text:fragment.normalized_fragment,source_span:fragment.span,source_axes:fragment.source_axes,assertion_type:type,verification_line:{passed,line:origin==='CODE_STRUCTURE'?'CODE_STRUCTURE_LINE':'NATURAL_ASSERTION_LINE'},unresolved:passed?[]:['claim_structure']});}
function claimsForTask(task){const claims=[],seen=new Set(),offset=Number(task.source_span?.start||0),premises=Array.isArray(task.premises)?task.premises.filter(Boolean):[],source=String(task.source_span?.text||task.raw_text||'');const inputs=premises.length?premises.map((text)=>({text,forced:true})):[{text:source,forced:false}];for(const input of inputs){for(const fragment of fragmentText(input.text,offset)){const claim=claimFromFragment(fragment,task,{forced:input.forced});if(!claim||seen.has(claim.claim_id))continue;seen.add(claim.claim_id);claims.push(claim);}}if(!claims.length&&task.evidence_need?.required){const text=normalizeText(task.target||task.source_span?.text||task.objective||'UNKNOWN'),payload={task_id:task.id,fragment_id:id('frag',{task_id:task.id,text}),subject:text||'UNKNOWN',predicate:'VERIFICATION_TARGET',object_or_value:'UNKNOWN',polarity:'AFFIRMATIVE',modality:'OBSERVED',time_scope:timeScope(text),jurisdiction:'UNKNOWN',version_scope:'UNKNOWN',claim_origin:'DIRECT_ASSERTION'};claims.push(deepFreeze({...payload,claim_id:id('claim',payload),raw_text:text,source_span:task.source_span||null,source_axes:{container_role:['TASK_TARGET'],content_role:['VERIFICATION_TARGET'],quotation_role:['DIRECT']},assertion_type:'VERIFICATION_TARGET',verification_line:{passed:text!=='UNKNOWN',line:'TASK_TARGET_LINE'},unresolved:text==='UNKNOWN'?['claim_structure','target']:[]}));}return deepFreeze(claims);}

function policyFor(claim){if(claim.claim_origin==='CODE_STRUCTURE'||claim.claim_origin==='ATTRIBUTED_ASSERTION')return deepFreeze({claim_policy_id:claim.claim_origin==='CODE_STRUCTURE'?'POLICY_CODE_STRUCTURE':'POLICY_ATTRIBUTION',external_search_required:false,planned_query_roles:[],required_scope_fields:['subject','predicate'],verifiable_modalities:['OBSERVED','POSSIBILITY','CONDITIONAL_RULE'],independence_requirement:'LOCAL_INPUT_ONLY',allowed_evidence_sources:['LOCAL_INPUT_EVIDENCE']});const roles=[QUERY_ROLES.OFFICIAL,QUERY_ROLES.PRIMARY,QUERY_ROLES.INDEPENDENT];if(claim.time_scope==='CURRENT')roles.push(QUERY_ROLES.VERSION);roles.push(QUERY_ROLES.COUNTER);if(/\d/.test(String(claim.object_or_value||'')))roles.push(QUERY_ROLES.NUMERIC_LOWER,QUERY_ROLES.NUMERIC_UPPER,QUERY_ROLES.NUMERIC_ALTERNATE);return deepFreeze({claim_policy_id:/\d/.test(String(claim.object_or_value||''))?'POLICY_NUMERIC_FACT':claim.time_scope==='CURRENT'?'POLICY_CURRENT_FACT':'POLICY_GENERAL_FACT',external_search_required:true,planned_query_roles:roles,required_scope_fields:['subject','predicate'],verifiable_modalities:['OBSERVED','POSSIBILITY','CONDITIONAL_RULE'],independence_requirement:'AUTHORITY_PLUS_INDEPENDENT_OR_TWO_FAMILIES',allowed_evidence_sources:['EXTERNAL_RETRIEVED_EVIDENCE']});}
function queryText(role,claim){const subject=normalizeText(claim.subject),predicate=normalizeText(['STATED_CONTENT','VERIFICATION_TARGET'].includes(claim.predicate)?'':claim.predicate),value=normalizeText(claim.object_or_value==='UNKNOWN'?'':claim.object_or_value),base=normalizeText([subject,predicate,value].filter(Boolean).join(' '));if(role===QUERY_ROLES.OFFICIAL)return`${base} 公式`.trim();if(role===QUERY_ROLES.PRIMARY)return`${base} 一次資料`.trim();if(role===QUERY_ROLES.INDEPENDENT)return`${base} 独立資料`.trim();if(role===QUERY_ROLES.VERSION)return`${base} version current`.trim();if(role===QUERY_ROLES.NUMERIC_LOWER)return`${subject} ${predicate} 下限`.trim();if(role===QUERY_ROLES.NUMERIC_UPPER)return`${subject} ${predicate} 上限`.trim();if(role===QUERY_ROLES.NUMERIC_ALTERNATE)return`${subject} ${predicate} 別値`.trim();if(role===QUERY_ROLES.COUNTER){if(claim.modality==='CONDITIONAL_RULE')return`${subject} ${predicate} 反例 条件不成立 例外`.trim();if(claim.polarity==='NEGATIVE')return`${subject} ${predicate} 肯定形`.trim();return`${subject} ${predicate} 否定形`.trim();}throw Object.assign(new Error(`unsupported query role: ${role}`),{code:'QUERY_ROLE_UNSUPPORTED'});}
function queriesForClaim(claim,policy,domainId=null){if(!policy.external_search_required)return deepFreeze([]);if(!policy.planned_query_roles.includes(QUERY_ROLES.COUNTER))throw Object.assign(new Error('counter query role is mandatory'),{code:'COUNTER_ROLE_REQUIRED_BY_DESIGN'});return deepFreeze(policy.planned_query_roles.map((role)=>{const text=normalizeText(queryText(role,claim));return{query_id:id('query',{claim_id:claim.claim_id,role,text,version:'v4-restored-1'}),claim_id:claim.claim_id,role,text,normalized_query_text:text,domain_id:domainId,query_template_version:'v4-restored-1'};}));}
function buildCanonicalTaskPlan(task,domain={}){const claims=claimsForTask(task),policyByClaim={},queries=[];for(const claim of claims){const policy=policyFor(claim);policyByClaim[claim.claim_id]=policy;queries.push(...queriesForClaim(claim,policy,domain.primary?.id||null));}return deepFreeze({schema_version:'astera.canonical-task-plan.v1',task_id:task.id,claims,policy_by_claim_id:policyByClaim,search_plan:{schema_version:'astera.upstream-search-plan.v1',planning_authority:'ASTERA_DECISION_MATERIALS_V4',task_id:task.id,query_template_version:'v4-restored-1',planned_query_roles:unique(queries.map((query)=>query.role)),queries}});}

function evidenceIdentity(item){return item.source_family_id||item.authority_id||item.source_id||item.id||item.url||'';}
function bindEvidence(claim,policy,compact){if(!policy.external_search_required)return claim.claim_origin==='CODE_STRUCTURE'||claim.claim_origin==='ATTRIBUTED_ASSERTION'?[{relation:'SUPPORTS',evidence_source:'LOCAL_INPUT_EVIDENCE',candidate_id:claim.fragment_id,source_family_id:`local:${claim.fragment_id}`}]:[];return(compact.evidence||[]).map((item)=>({item,overlap:tokenOverlap(claim.raw_text||`${claim.subject} ${claim.object_or_value}`,item.claim||'')})).filter((entry)=>entry.overlap>=0.28).sort((a,b)=>b.overlap-a.overlap).map((entry)=>({relation:'SUPPORTS',evidence_source:'EXTERNAL_RETRIEVED_EVIDENCE',candidate_id:entry.item.id,source_role:entry.item.source_role,source_family_id:entry.item.source_family_id,authority_id:entry.item.authority_id,url:entry.item.url,overlap:Number(entry.overlap.toFixed(3))}));}
function independence(policy,bindings){if(!policy.external_search_required)return bindings.some((item)=>item.evidence_source==='LOCAL_INPUT_EVIDENCE');const external=bindings.filter((item)=>item.evidence_source==='EXTERNAL_RETRIEVED_EVIDENCE'),authorities=new Set(external.filter((item)=>['OFFICIAL','PRIMARY'].includes(item.source_role)).map(evidenceIdentity).filter(Boolean)),independent=new Set(external.filter((item)=>!['OFFICIAL','PRIMARY'].includes(item.source_role)).map(evidenceIdentity).filter(Boolean)),families=new Set(external.map(evidenceIdentity).filter(Boolean));return(authorities.size>=1&&independent.size>=1)||families.size>=2;}
function retrievalGate(policy,raw,compact){if(!policy.external_search_required)return true;const planned=new Set((raw?.planned_query_roles||raw?.query_plan?.planned_query_roles||[]).map(String)),rolesPresent=policy.planned_query_roles.every((role)=>planned.has(role)),runs=[...(raw?.provider_execution?.initial||[]),...(raw?.provider_execution?.reinforcement||[])],failures=runs.filter((run)=>run.status&&!['FULFILLED','NOT_FOUND'].includes(String(run.status).toUpperCase())),authority=String(raw?.planning_authority||raw?.query_plan?.planning_authority||'');return rolesPresent&&failures.length===0&&authority==='UPSTREAM_CANONICAL'&&compact.state==='VALID';}
function evaluateClaim(claim,policy,raw){const compact=normalizeEvidencePacket(raw),bindings=deepFreeze(bindEvidence(claim,policy,compact)),missing=policy.required_scope_fields.filter((field)=>claim[field]==null||String(claim[field]).trim()===''||String(claim[field])==='UNKNOWN'),gates={G1:claim.verification_line?.passed===true,G2:policy.verifiable_modalities.includes(claim.modality),G3:missing.length===0,G4:independence(policy,bindings),G5:retrievalGate(policy,raw,compact),G6:compact.conflict_detected!==true,G7:claim.claim_origin==='DIRECT_ASSERTION'?bindings.some((binding)=>binding.evidence_source==='EXTERNAL_RETRIEVED_EVIDENCE'):bindings.some((binding)=>binding.evidence_source==='LOCAL_INPUT_EVIDENCE')},status=Object.values(gates).every(Boolean)?CONFIRMED:UNDETERMINED,reasons=[];if(!gates.G1)reasons.push('G1_VERIFICATION_LINE_INCOMPLETE');if(!gates.G2)reasons.push('G2_MODALITY_NOT_VERIFIABLE');if(!gates.G3)reasons.push(`G3_SCOPE_UNKNOWN:${missing.join(',')}`);if(!gates.G4)reasons.push('G4_EVIDENCE_OR_INDEPENDENCE_INSUFFICIENT');if(!gates.G5)reasons.push('G5_PLANNED_QUERY_RETRIEVAL_INCOMPLETE_OR_INVALID');if(!gates.G6)reasons.push('G6_UNRESOLVED_CONFLICT');if(!gates.G7)reasons.push('G7_ORIGIN_CONFIRMABILITY_NOT_SATISFIED');return deepFreeze({claim_id:claim.claim_id,task_id:claim.task_id,status,reasons,gates,missing_scope_fields:missing,supported_scope:{subject:claim.subject,property:claim.predicate,time:claim.time_scope,jurisdiction:claim.jurisdiction,version:claim.version_scope},bindings,evidence_state:compact.state,evidence_quality_status:compact.source_status,evidence_quality_score_bp:compact.quality_score_bp});}
function evaluateCanonicalTaskPlan(plan,rawEvidence){const records=plan.claims.map((claim)=>{const policy=plan.policy_by_claim_id[claim.claim_id];return deepFreeze({claim,policy,confirmation:evaluateClaim(claim,policy,rawEvidence)});});return deepFreeze({schema_version:'astera.canonical-claim-records.v1',task_id:plan.task_id,search_plan:plan.search_plan,records,confirmed_count:records.filter((record)=>record.confirmation.status===CONFIRMED).length,undetermined_count:records.filter((record)=>record.confirmation.status===UNDETERMINED).length});}

function domainArray(domain,names){for(const name of names)if(Array.isArray(domain?.primary?.[name]))return domain.primary[name];return[];}
function projectFiveLanes({task,canonical,domain={}}){const records=canonical?.records||[],confirmed=records.filter((record)=>record.confirmation.status===CONFIRMED),undetermined=records.filter((record)=>record.confirmation.status===UNDETERMINED),riskLens=domainArray(domain,['risk_lens','risk_checks','risks','safety_checks']),multiLens=domainArray(domain,['multi_lens','perspectives']),compareLens=domainArray(domain,['compare_lens','comparison_dimensions']),evidenceNeeds=domainArray(domain,['evidence_to_collect','required_evidence']),hardConstraints=unique([...(task.constraints||[]),...(task.prohibitions||[]),...(task.preserve||[])]),riskItems=[...riskLens.map((item,index)=>({key:`domain-risk-${index+1}`,impact:String(item),weight:20,source:'DOMAIN_LENS'})),...(task.hard_blockers||[]).map((item,index)=>({key:`hard-blocker-${index+1}`,impact:String(item),weight:100,source:'TASK_GRAPH'}))];const fact={lane:'fact',confirmed_claim_ids:confirmed.map((record)=>record.claim.claim_id).sort(),undetermined_claim_ids:undetermined.map((record)=>record.claim.claim_id).sort(),confirmed:confirmed.map((record)=>({claim_id:record.claim.claim_id,text:record.claim.raw_text,status:CONFIRMED,supported_scope:record.confirmation.supported_scope,bindings:record.confirmation.bindings})),unconfirmed:undetermined.map((record)=>({claim_id:record.claim.claim_id,text:record.claim.raw_text,status:UNDETERMINED,reasons:record.confirmation.reasons})),opinions:[],evidence_need:undetermined.map((record)=>({item:record.claim.raw_text,reason:record.confirmation.reasons.join(' / ')})),evidence_gaps:undetermined.map((record)=>({item:record.claim.raw_text,reason:record.confirmation.reasons.join(' / ')}))};const risk={lane:'risk',source:'CANONICAL_CLAIM_RECORDS_AND_DOMAIN_RULES',risk_count:riskItems.length,risks:riskItems,highest:riskItems[0]||null,unresolved_claim_count:undetermined.length,hard_constraints:hardConstraints,safety_gates:unique(task.prohibitions||[]),level:(task.hard_blockers||[]).length?'high':riskLens.length?'medium':'low'};const multi={lane:'multi',perspectives:multiLens.map((item,index)=>({id:`domain-perspective-${index+1}`,focus:String(item),source:'DOMAIN_LENS',claim_ids:confirmed.map((record)=>record.claim.claim_id)})),supported_scopes:confirmed.map((record)=>({claim_id:record.claim.claim_id,...record.confirmation.supported_scope})),trade_off_map:multiLens.map((item,index)=>({id:`domain-perspective-${index+1}`,dimension:String(item),status:'MATERIAL_ONLY'}))};const inquiry={lane:'inquiry',problem_health:{healthy:undetermined.length===0&&!(task.hard_blockers||[]).length,reason:undetermined.length?'Canonical Claims remain UNDETERMINED.':'Canonical Claims are traceable.'},missing_fields:unique(undetermined.flatMap((record)=>record.confirmation.missing_scope_fields.map((field)=>`${record.claim.claim_id}:${field}`))),missing_questions:unique([...undetermined.flatMap((record)=>record.confirmation.reasons),...evidenceNeeds]),open_items:undetermined.map((record)=>({claim_id:record.claim.claim_id,reasons:record.confirmation.reasons,missing_scope_fields:record.confirmation.missing_scope_fields}))};const denominator=records.length,compare={lane:'compare',material_only:true,counts:{claims:records.length,confirmed:confirmed.length,undetermined:undetermined.length,conflicts:undetermined.filter((record)=>record.confirmation.reasons.includes('G6_UNRESOLVED_CONFLICT')).length},coverage:denominator?{numerator:confirmed.length,denominator,ratio:confirmed.length/denominator}:null,dimensions:compareLens.map(String),scope_booleans:records.map((record)=>({claim_id:record.claim.claim_id,required_scope_defined:record.policy.required_scope_fields.length>0,all_required_scope_confirmed:record.confirmation.status===CONFIRMED&&record.confirmation.missing_scope_fields.length===0})),selected_candidate:null,candidate_ranking:[],rejected_candidates:[],verdict:{decision:'MATERIAL_ONLY',reason:'Astera does not select, rank, recommend, adopt, or reject candidates.',objective:task.objective||''}};return deepFreeze({fact,risk,multi,inquiry,compare});}
function deterministicPerspectiveExpansion({task,canonical,domain={}}){const records=canonical?.records||[],confirmed=records.filter((record)=>record.confirmation.status===CONFIRMED).length,undetermined=records.length-confirmed;return deepFreeze({engine:'Astera Deterministic Perspective Expansion',mode:'MATERIAL_ONLY',candidates:[],selected:null,rejected:[],perspectives:[{id:'forward',focus:task.objective||task.target||'',basis:{confirmed,undetermined,rule_id:'PERSPECTIVE-FORWARD-MATERIAL'}},{id:'counter',focus:'Counter-evidence and failure conditions',basis:{query_role:QUERY_ROLES.COUNTER,rule_id:'PERSPECTIVE-COUNTER-MATERIAL'}},{id:'constraint',focus:unique([...(task.constraints||[]),...(task.prohibitions||[]),...(task.preserve||[])]),basis:{rule_id:'PERSPECTIVE-CONSTRAINT-MATERIAL'}},{id:'domain',focus:domainArray(domain,['multi_lens','perspectives']).map(String),basis:{lens_id:domain.primary?.id||null,rule_id:'PERSPECTIVE-DOMAIN-MATERIAL'}}]});}

module.exports={QUERY_ROLES,CONFIRMED,UNDETERMINED,fragmentText,claimsForTask,policyFor,queriesForClaim,buildCanonicalTaskPlan,evaluateCanonicalTaskPlan,projectFiveLanes,deterministicPerspectiveExpansion};
