{
  "name": "@astera/quality-completion-evaluator",
  "version": "1.0.0",
  "description": "ASTERA internal deterministic quality/completion scorer with shared 38-genre lenses and KB admission gate",
  "main": "index.js",
  "type": "commonjs",
  "bin": {
    "astera-qce": "./cli/evaluate.js"
  },
  "scripts": {
    "test": "node --test tests/**/*.test.js",
    "test:unit": "node --test tests/unit/*.test.js",
    "test:boundary": "node --test tests/boundary/*.test.js",
    "test:integration": "node --test tests/integration/*.test.js",
    "test:regression": "node --test tests/regression/*.test.js",
    "smoke": "node examples/create-sample-request.js | node cli/evaluate.js",
    "verify": "node --check index.js && node --check evaluator-engine.js && node --check domain-lens-resolver.js && npm test && npm run smoke"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "license": "UNLICENSED",
  "private": true
}
